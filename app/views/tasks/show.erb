<% provide(:title, 'Tarefas') %>
<% provide(:header, "#{@task.task_name.capitalize} [#{@task.platform_name}]") %>
<% provide(:description, "Detalhes sobre a tarefa.") %>
<% provide(:levels, 'Tarefas, Detalhes') %>

<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title">Detalhes</h3>
  </div><!-- /.box-header -->
  <%= render partial: 'task', locals: { task: @task } %>
</div><!-- /.box -->